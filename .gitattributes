# テキストは自動判定し、リポジトリ内では LF に正規化する(環境差による diff ノイズを防ぐ)。
* text=auto eol=lf

# Windows 専用のスクリプトは CRLF を維持。
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# バイナリ資産(EOL 変換しない)。
*.wav binary
*.png binary
*.ico binary
