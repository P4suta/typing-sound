# Auto-detect text and normalize to LF in the repo (avoids cross-platform diff noise).
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets (no EOL conversion).
*.wav binary
*.png binary
*.ico binary
