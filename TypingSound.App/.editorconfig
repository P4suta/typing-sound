# App (WinUI) project-specific analyzer tweaks.
# XAML-derived types (App / Window / Page ...) are source-generated as public partial,
# so public is a framework requirement and they cannot be internal. CA1515 ignores this,
# so disable it in the UI project only (core logic quality rules stay strict).
[*.cs]
dotnet_diagnostic.CA1515.severity = none
